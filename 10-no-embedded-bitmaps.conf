<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

  <!-- Disable embedded bitmaps by default -->
  <match target="pattern">
    <edit name="embeddedbitmap" mode="append">
      <bool>false</bool>
    </edit>
  </match>

</fontconfig>
